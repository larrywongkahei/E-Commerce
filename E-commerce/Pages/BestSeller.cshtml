@page
@model BestSellerModel
@{ 
    int countSmartphone = 0;
    int countLaptops = 0;
    int countFragrances = 0;
    int countSkincare = 0;
    int countGroceries = 0;
    int countHomeDecoration = 0;
}
<div class="" style="margin-left: 30%;">
    <h2 style="font-weight: 700; font-size:24px;">
                    Best Sellers in Smartphones
                </h2>
                <div id="Smartphones" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                    <div class="carousel-inner" style="width:100%">
                        @for (var i = Model.SmartPhones.First().Id; i < Model.SmartPhones.Count(); i += 3)
                                {
                        <div class="@(countSmartphone == 0 ? "carousel-item active" : "carousel-item") " style="">
                           <div class="d-flex" style="justify-content: space-evenly">
                               <div class="">
                                   <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                   <p>@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
        <p class="card-text" style=" width: 230px">@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                   @for (var x = 0; x < 5; x++)
                                {
                                    <span class="@(x <= Model.SmartPhones.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                }
                                   <p>£@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                               </div>
                               <div>
                                   <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                   <p>@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 2)?.Title</p>

                                   <p class="card-text" style=" width: 230px">@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                   @for (var x = 0; x < 5; x++)
                                   {
                                       <span class="@(x <= Model.SmartPhones.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                   }
                                   <p>£@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                               </div>
                               <div>
                                   <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                   <p>@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 3)?.Title</p>

                                   <p class="card-text" style=" width: 230px">@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                   @for (var x = 0; x < 5; x++)
                                   {
                                       <span class="@(x <= Model.SmartPhones.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                   }
                                   <p>£@Model.SmartPhones.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                               </div>
                            </div>
                        </div>
                                countSmartphone++;
                                }
                            </div>
                    <a class="carousel-control-prev" href="#Smartphones" role="button" data-slide="prev" style="justify-content: left !important;">
                        <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#Smartphones" role="button" data-slide="next" style="justify-content: right !important;">
                        <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>

            <h2 style="font-weight: 700; font-size:24px;">
                    Best Sellers in Laptops
                </h2>
                <div id="Laptops" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                    <div class="carousel-inner" style="width:100%">
                        @for (var i = Model.LapTops.First().Id; i < Model.LapTops.First().Id + 5; i += 3)
                        {
                            <div class="@(countLaptops == 0 ? "carousel-item active" : "carousel-item") " style="">

                                <div class="d-flex" style="justify-content: space-evenly">
                                    <div class="">
                                        <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.LapTops.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                        <p>@Model.LapTops.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
                                        <p class="card-text" style=" width: 230px">@Model.LapTops.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                        @for (var x = 0; x < 5; x++)
                                        {
                                            <span class="@(x <= Model.LapTops.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                        }
                                        <p>£@Model.LapTops.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                                    </div>
                                    <div>
                                        <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.LapTops.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                        <p>@Model.LapTops.FirstOrDefault(product => product.Id == i + 2)?.Title</p>
                                        <p class="card-text" style=" width: 230px">@Model.LapTops.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                        @for (var x = 0; x < 5; x++)
                                        {
                                            <span class="@(x <= Model.LapTops.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                        }
                                        <p>£@Model.LapTops.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                                    </div>
                                    <div>
                                        <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.LapTops.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                        <p>@Model.LapTops.FirstOrDefault(product => product.Id == i + 3)?.Title</p>
                                        <p class="card-text" style=" width: 230px">@Model.LapTops.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                        @for (var x = 0; x < 5; x++)
                                        {
                                            <span class="@(x <= Model.LapTops.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                        }
                                        <p>£@Model.LapTops.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                                    </div>
                                </div>
                            </div>
                        countLaptops++;
                        }
                    </div>
                    <a class="carousel-control-prev" href="#Laptops" role="button" data-slide="prev" style="justify-content: left !important;">
                        <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#Laptops" role="button" data-slide="next" style="justify-content: right !important;">
                        <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            <h2 style="font-weight: 700; font-size:24px;">
                Best Sellers in Fragrances
            </h2>
            <div id="Fragrances" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                <div class="carousel-inner" style="width:100%">
                    @for (var i = Model.Fragrances.First().Id; i < Model.Fragrances.First().Id + 5; i += 3)
                    {
                        <div class="@(countFragrances == 0 ? "carousel-item active" : "carousel-item") " style="">

                            <div class="d-flex" style="justify-content: space-evenly">
                                <div class="">
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Fragrances.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                    <p>@Model.Fragrances.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Fragrances.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Fragrances.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Fragrances.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Fragrances.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                    <p>@Model.Fragrances.FirstOrDefault(product => product.Id == i + 2)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Fragrances.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Fragrances.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Fragrances.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.Fragrances.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                    <p>@Model.Fragrances.FirstOrDefault(product => product.Id == i + 3)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Fragrances.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Fragrances.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Fragrances.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                                </div>
                            </div>
                        </div>
                        countFragrances++;
                    }

                </div>
                <a class="carousel-control-prev" href="#Fragrances" role="button" data-slide="prev" style="justify-content: left !important;">
                    <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#Fragrances" role="button" data-slide="next" style="justify-content: right !important;">
                    <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
            <h2 style="font-weight: 700; font-size:24px;">
                Best Sellers in Skincare
            </h2>
            <div id="Skincare" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                <div class="carousel-inner" style="width:100%">
                    @for (var i = Model.Skincare.First().Id; i < Model.Skincare.First().Id + 5; i += 3)
                    {
                        <div class="@(countSkincare == 0 ? "carousel-item active" : "carousel-item") " style="">

                            <div class="d-flex" style="justify-content: space-evenly">
                                <div class="">
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Skincare.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                    <p>@Model.Skincare.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Skincare.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Skincare.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Skincare.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Skincare.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                    <p>@Model.Skincare.FirstOrDefault(product => product.Id == i + 2)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Skincare.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Skincare.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Skincare.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.Skincare.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                    <p>@Model.Skincare.FirstOrDefault(product => product.Id == i + 3)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Skincare.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Skincare.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Skincare.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                                </div>
                            </div>
                        </div>
                    countSkincare++;
                    }
                </div>
                <a class="carousel-control-prev" href="#Skincare" role="button" data-slide="prev" style="justify-content: left !important;">
                    <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#Skincare" role="button" data-slide="next" style="justify-content: right !important;">
                    <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
            <h2 style="font-weight: 700; font-size:24px;">
                Best Sellers in Groceries
            </h2>
            <div id="Groceries" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                <div class="carousel-inner" style="width:100%">
                    @for (var i = Model.Groceries.First().Id; i < Model.Groceries.First().Id + 5; i += 3)
                    {
                        <div class="@(countGroceries == 0 ? "carousel-item active" : "carousel-item") " style="">

                            <div class="d-flex" style="justify-content: space-evenly">
                                <div class="">
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Groceries.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                    <p>@Model.Groceries.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Groceries.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Groceries.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Groceries.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.Groceries.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                    <p>@Model.Groceries.FirstOrDefault(product => product.Id == i + 2)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Groceries.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Groceries.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Groceries.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.Groceries.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                    <p>@Model.Groceries.FirstOrDefault(product => product.Id == i + 3)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.Groceries.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.Groceries.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.Groceries.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                                </div>
                            </div>
                        </div>
                    countGroceries++;
                    }
                </div>
                <a class="carousel-control-prev" href="#Groceries" role="button" data-slide="prev" style="justify-content: left !important;">
                    <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#Groceries" role="button" data-slide="next" style="justify-content: right !important;">
                    <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
            <h2 style="font-weight: 700; font-size:24px;">
                Best Sellers in HomeDecoration
            </h2>
            <div id="HomeDecoration" class="carousel slide" style="text-align: center;" data-ride="carousel" data-interval=false>
                <div class="carousel-inner" style="width:100%">
                    @for (var i = Model.HomeDecoration.First().Id; i < Model.HomeDecoration.First().Id + 5; i += 3)
                    {
                        <div class="@(countHomeDecoration == 0 ? "carousel-item active" : "carousel-item") " style="">
                            <div class="d-flex" style="justify-content: space-evenly">
                                <div class="">
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 1)?.Thumbnail" />
                                    <p>@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 1)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 1)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 1)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 1)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block" style="height: 160px; width: 230px" src="@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 2)?.Thumbnail" />
                                    <p>@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 2)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 2)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 2)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 2)?.Price</p>
                                </div>
                                <div>
                                    <img clsas="d-block " style="height: 160px; width: 230px" src="@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 3)?.Thumbnail" />
                                    <p>@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 3)?.Title</p>
                                    <p class="card-text" style=" width: 230px">@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 3)?.Description</p>
                                    @for (var x = 0; x < 5; x++)
                                    {
                                        <span class="@(x <= Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 3)?.Rating ? "fa fa-star checked rating bottom-0" : "far fa-star rating")"></span>
                                    }
                                    <p>£@Model.HomeDecoration.FirstOrDefault(product => product.Id == i + 3)?.Price</p>
                                </div>
                            </div>
                        </div>
                    countHomeDecoration++;
                    }
                </div>
                <a class="carousel-control-prev" href="#HomeDecoration" role="button" data-slide="prev" style="justify-content: left !important;">
                    <span class="fa-solid fa-circle-arrow-left fa-2xl" aria-hidden="true" style="color: black;"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#HomeDecoration" role="button" data-slide="next" style="justify-content: right !important;">
                    <span class="fa-solid fa-circle-arrow-right fa-2xl" style="color: black;" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
