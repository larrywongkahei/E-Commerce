@page
@model E_commerce.Pages.GroceriesModel
@{
}

<div class="d-flex">
    <div style="width: 20%;">
        Here is for selector
    </div>
    <div class="d-flex flex-wrap" style="width: 80%">
        @foreach (var product in Model.GroceriesFromAPI)
        {
            <div class="card">
                <img class="card-img-top" src="@product.Images.Last()" />
                <div class="card-body">
                    <p class="card-text" style="font-size: 16px; height: 3rem;">@product.Title</p>
                    <div>
                        @for (var i = 0; i < 5; i++)
                        {
                            <span class="@(i <= product.Rating ? "fa fa-star checked rating " : "far fa-star rating")"></span>
                        }
                    </div>
                    <p class="">£@product.Price</p>
                </div>
            </div>
        }
    </div>
</div>
